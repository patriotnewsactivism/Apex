# APEX Capability Matrix

_Companion to `docs/APEX_SYSTEM_AUDIT.md`. Statuses: **WORKING** (live and used in production), **PARTIAL** (implemented but incomplete, unverified live, or gated off), **BROKEN** (confirmed non-functional), **MISSING** (no implementation), **UNKNOWN** (could not be verified without live credentials/access this audit did not have), **DUPLICATED** (implemented more than once with different completeness)._

| Capability | Responsible project | Implementation location | Status | Dependencies | Exposed interface | Persistence | Automation suitability | Missing work | Recommended owner after integration |
|---|---|---|---|---|---|---|---|---|---|
| Goal intake & decomposition | Apex | `packages/agents/src/apex-ceo.ts`, `packages/core/src/task-queue.ts` | WORKING | Postgres, OpenRouter | `POST /api/goals`, CEO chat (`/api/chat`) | Postgres (`goals`, `tasks`) | High — deterministic state machine, LLM only for decomposition | Route-level tests | Apex |
| Task/goal persistence & lifecycle | Apex | `lib/db/src/schema.ts`, `packages/core/src/task-queue.ts` | WORKING | Postgres | internal | Postgres | High | Multi-instance claim safety under real load (tracked in `CHECKLIST.md`) | Apex |
| Per-tool approval gating | Apex | `packages/core/src/tool-registry.ts` (`requiresApproval`), `base-agent.ts` (`requestHumanApproval`) | WORKING | Postgres | `/api/approvals` | Postgres (`approvals`) | N/A — this *is* the automation-safety control | Per-agent `approvalRequired` flag is defined but not read anywhere in the live path (only per-tool gating is enforced) — confirm intended vs. dead field | Apex |
| Retry/backoff/dead-letter (tasks) | Apex | `packages/core/src/task-queue.ts`, `provider-failure.ts` | WORKING | Postgres | internal | Postgres | High | none found | Apex |
| Scheduled/recurring jobs | Apex | `packages/background-jobs` (`JobScheduler`, `CronParser`, `JobExecutor`) | WORKING | Postgres | `/api/jobs` | Postgres (`scheduledJobs`, `jobExecutionLog`) | High | Multi-instance atomic claim load-test (tracked in `CHECKLIST.md`) | Apex |
| Browser-independent durable execution | Apex | `packages/api-server/src/worker.ts` (`start:worker`) | PARTIAL | Postgres, verified Cloud Run topology | none (headless) | Postgres | High once topology verified | ADR-011's own acceptance test (no-browser production proof) is defined but not confirmed executed against live Cloud Run — verification status UNKNOWN, requires live GCP access | Apex |
| Sales/lead campaign engine | Apex | `packages/background-jobs/src/campaign-runner.ts`, `leadCampaigns`/`campaignSegments` tables, `/api/campaigns` | PARTIAL | Postgres, lead-source APIs (Yelp/Google Places/Tavily/Brave/Firecrawl) | `/api/campaigns`, `/api/leads` | Postgres | Medium — real external side effects need provider-specific rate/consent handling | End-to-end lead → contact → qualify → CRM handoff verification (tracked as open in `ROADMAP.md` P4); this audit did not independently re-verify it live | Apex |
| Social/content moderation (community-watch) | Apex | `packages/community-watch` (`classify.ts`, `draft.ts`) | PARTIAL | OpenRouter | none — library only, not wired to a route or scheduler | none | Advisory-only by design (human posts) | Not invoked by anything yet — no route, no scheduled job calls it | Apex |
| Health monitoring | Apex | `packages/health-monitor` | WORKING | Postgres, LLM providers | `/api/health/*` | Postgres (`healthMetrics`, `componentHealth`) | High | none found | Apex |
| Learning/continuous improvement | Apex | `packages/learning-system` | WORKING | Postgres, task outcomes | `/api/learning/*` | Postgres (`taskOutcomes`, `learningInsights`, `strategyRecommendations`) | High | none found | Apex |
| Predictive forecasting | Apex | `packages/predictive` | WORKING | Postgres | `/api/predictive/*` | Postgres | Medium | No test coverage | Apex |
| Cross-portfolio orchestration | Apex | `packages/multiapp` | PARTIAL | Postgres, external product APIs | `/api/applications/*` | Postgres (`applications`, `applicationTasks`) | Medium | `OrchestrationEngine` is thin (27 lines) — real delegation logic lives in `packages/core/src/orchestration-tools.ts` instead; the split is confusing and worth documenting or consolidating | Apex |
| CI/CD automation (real) | Apex | `packages/cicd-automation`, `cloud-run-deployer.ts` | WORKING | gcloud/GCB credentials | tool-registry (`run_test`/`run_lint`/`build_project`/`deploy_to_environment`/`rollback_deployment`) | Postgres (`deployments`) | High, approval-gated for deploy/rollback | `deploy_to_environment`'s tool description/schema referenced retired AWS Lightsail — **fixed in this audit** | Apex |
| CI/CD automation (Convex path) | Apex | `packages/cicd-worker`, `packages/convex-backend/convex/cicd.ts` | BROKEN by design | Convex (experimental, `APEX_CONVEX_AUTONOMY_ENABLED=false`) | none in production | Convex `externalJobs` | N/A | Its own `handleDeploy`/`handleRollback` always throw; carried a stale AWS-Lightsail runbook comment invisible to the retired-hosting-instructions guard (only scans `.md`) — **fixed in this audit**. Decide: finish the Convex migration, or delete this path. | Undecided — needs an explicit product decision (see Architecture doc) |
| Multi-provider LLM routing | Apex | `packages/core/src/llm-client.ts`, `model-routing.ts`, `model-intelligence.ts` | WORKING | OpenRouter | internal, `/api/settings` | Postgres (policy, telemetry) | High — this is exactly the "use the right model for the job" mechanism the platform needs | none found | Apex |
| Convex parallel backend | Apex | `packages/convex-backend` | PARTIAL/experimental | Convex | none in production | Convex | N/A until a decision is made | Behind live Postgres schema (missing 3 tables); per-agent approval bypass logic diverges from the live path | Undecided (ADR-008: not production authority) |
| Deprecated agent workforce (predecessor) | Apex-Agent | entire repo | BROKEN, abandoned | Railway (retired), SQLite | none live | SQLite (local) | N/A | Formal deprecation notice; no further development recommended | None — recommend archiving with a pointer to Apex |
| Multi-agent monitoring orchestrator | Apex-Stream | `services/orchestrator` | WORKING (Cloud Run) | Postgres, AWS SQS/EventBridge/Cognito | REST + `/api/audit/verify` | Postgres | High for the orchestrator itself | Auth/dispatch still AWS-coupled (see below) | Apex-Stream |
| Feed/API ingestion agent (Aria) | Apex-Stream | `services/agent-aria` | PARTIAL | AWS SQS, Postgres | none (pull-based) | Postgres | High once deployable | No deploy path in current CI | Apex-Stream |
| Web-page diff/ingestion agent (Atlas) | Apex-Stream | `services/agent-atlas` | PARTIAL | AWS SQS, Postgres | none (pull-based) | Postgres | High once deployable | No deploy path in current CI | Apex-Stream |
| Live-stream capture agent (Sentinel) | Apex-Stream | `services/agent-sentinel` | PARTIAL | AWS SQS, S3, Postgres | none (pull-based), on-demand launch via ECS `RunTaskCommand` | Postgres + S3 | High once deployable | No deploy path; ECS launch dependency is itself AWS-only | Apex-Stream |
| Evidence archival agent (Archivist) | Apex-Stream | `services/agent-archivist` | PARTIAL | AWS SQS, S3 (Object Lock + KMS), Postgres | none (pull-based) | Postgres + S3 | High once deployable, compliance-critical | No deploy path; no non-AWS evidence-storage path | Apex-Stream |
| YouTube comment triage agent (Warden) | Apex-Stream | `services/agent-warden` | PARTIAL | YouTube Data API, AWS Bedrock (classify/draft), Postgres | `/api/replies/*` (via orchestrator) | Postgres | High — approve-first design is sound | No deploy path; no `lambda.ts` (only agent missing dual-mode, though dual-mode itself is vestigial) | Apex-Stream |
| Declarative workflow engine | Apex-Stream | `packages/workflow-engine` | WORKING | none (pure functions + injected effects) | `/api/workflows/*` | Postgres (`workflows`, `workflow_executions`) | High — dry-run mode makes this safe to automate against | none found; genuinely well-tested (13 of the 44 passing tests) | Apex-Stream |
| Hash-chained audit log | Apex-Stream | `packages/core/src/audit` | WORKING | Postgres (advisory lock) | `/api/audit/verify` | Postgres (`audit_log`) | N/A — integrity primitive | none found | Apex-Stream |
| Anomaly scoring engine | Apex-Stream | `packages/core/src/scoring` | WORKING | none (pure functions) | internal | none (computed) | High — deterministic, explainable, well-tested | none found | Apex-Stream |
| RBAC / permission model | Apex-Stream | `packages/core/src/security/rbac.ts` | WORKING | AWS Cognito (group claims) | internal | none | N/A | Auth backend is AWS-only; RBAC *logic* itself is portable | Apex-Stream |
| Envelope encryption / evidence integrity | Apex-Stream | `packages/core/src/security/crypto.ts` | WORKING | Injectable `DataKeyProvider` (KMS today) | internal | n/a | N/A | Already KMS-agnostic by design — the provider is swappable; only the AWS KMS *implementation* is AWS-specific | Apex-Stream |
| Notification delivery | Apex-Stream | `services/orchestrator/src/effects.ts` (`notify()`) | BROKEN | none wired | `/api/notifications` (read-only) | Postgres (`notifications`, status stuck at `queued`) | N/A until fixed | No delivery mechanism exists on any platform; declared AWS SNS/SES deps are unused dead code | Apex-Stream — needs a provider decision |
| Dispatch/eventing (SQS + EventBridge) | Apex-Stream | `packages/agent-runtime/src/bus.ts` | WORKING but AWS-only | AWS SQS, EventBridge | internal | AWS (not Postgres) | High once/if a Cloud-native equivalent exists | No Postgres/Pub-Sub alternative exists anywhere | Apex-Stream — architecture decision needed |
| Operator authentication (Cognito) | Apex-Stream | `services/orchestrator/src/auth.ts`, `.github/workflows/create-operator.yml` | WORKING but AWS-only | AWS Cognito | JWT bearer | AWS Cognito | High once/if a Cloud-native equivalent exists | No alternative identity provider or account-provisioning path exists at all | Apex-Stream — architecture decision needed |
| Evidence storage (S3 Object Lock + KMS) | Apex-Stream | `services/agent-archivist/src/vault.ts`, `services/agent-sentinel/src/stream.ts` | WORKING but AWS-only | AWS S3, KMS | internal | AWS S3 | High once/if a Cloud-native equivalent exists | No non-AWS evidence-storage path; this is the most compliance-sensitive gap in the whole ecosystem | Apex-Stream — architecture decision needed |
| Agent memory (DynamoDB) | Apex-Stream | `packages/agent-runtime/src/memory.ts` | WORKING but AWS-only, **undisclosed in the repo's own README** | AWS DynamoDB | internal | AWS DynamoDB | High once/if a Cloud-native equivalent exists | No alternative exists; not even mentioned in the migration-status doc before this audit | Apex-Stream — architecture decision needed |
| Database layer (Postgres) | Apex-Stream | `packages/agent-runtime/src/sql-pg.ts` | WORKING, genuinely migrated | Postgres (Cloud SQL or equivalent) | internal | Postgres | High — this is the model the other subsystems should follow | Was missing its TLS CA bundle — **fixed in this audit** | Apex-Stream |
| Cross-repo integration (Apex ↔ Apex-Stream) | Both | none at runtime; one static code port (`community-watch`) | MISSING | — | — | — | — | No API contract, no shared auth, no event flow from Apex-Stream findings into Apex goals/tasks | New, thin integration layer — see `docs/APEX_ARCHITECTURE.md` |

## Notes on "DUPLICATED" capabilities

| Capability | Duplicated in | Recommended resolution |
|---|---|---|
| Hierarchical agent workforce concept | Apex (`packages/agents`+`packages/core`), Apex-Agent (entire repo) | Apex is authoritative; Apex-Agent deprecated (see Architecture doc) |
| "Orchestration" naming | Apex `packages/orchestrator` (dead stub), `packages/multiapp`, `packages/core/src/orchestration-tools.ts` | Delete `packages/orchestrator` (its own comment says it's safe to); keep the multiapp/orchestration-tools split but document which owns what |
| CI/CD automation | Apex `packages/cicd-automation` (live) vs. `packages/cicd-worker`+Convex (dead) | Decide whether the Convex migration continues; if not, delete the worker path rather than leave it as a landmine |
| `rds-ca.ts` TLS loader | Apex-Stream orchestrator (dead copy) + `agent-warden` (live copy) | Consolidated into `packages/agent-runtime` in this audit |
| `ARCHITECTURE.md`/`architecture.md`, `SECURITY.md`/`security.md` | Apex-Stream `docs/` | De-duplicated in this audit |
| Orphaned `packages/frontend` | Both Apex and Apex-Agent | Dead in both; safe to delete once independently confirmed unreferenced by any build step (Apex's Dockerfile already excludes it explicitly) |

---

_See `docs/APEX_ARCHITECTURE.md` for how these pieces should relate going forward, and `docs/APEX_IMPLEMENTATION_PROGRESS.md` for what was actually changed._
